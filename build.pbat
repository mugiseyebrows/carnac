
def main
    github_checkout()
    dotnet new tool-manifest
    dotnet tool install Cake.Tool --version 4.0.0
    
def build shell pwsh
    ./build.ps1

def test
    use(conda)
    install(mugicli)
    pyfind

order main build test

github-workflow 1